"""LangGraph + Playwright agent core.

This package implements an observe → think → act loop where the LLM can only
interact with the page using integer element IDs generated during observation.
"""

